Place your live extension manifests in `/extensions/*.json`.

These files are examples only, so Jarvis does not auto-load them.

You can copy one of the example files into `/extensions/` and then:

1. edit the values you need
2. run Jarvis
3. call `extensions:reload` from the internal tool layer later if you wire that into UI
