{
  "name": "jarvis-install-web",
  "private": true,
  "scripts": {
    "sync": "node ../../scripts/sync-install-site-config.cjs",
    "dev": "npm run sync && wrangler pages dev public",
    "deploy": "node ../../scripts/deploy-install-site.cjs",
    "deploy:ci": "node ../../scripts/deploy-install-site.cjs"
  },
  "devDependencies": {
    "wrangler": "^4.83.0"
  }
}
